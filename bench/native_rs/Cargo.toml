[package]
name = "mpkernels"
version = "0.1.0"
edition = "2021"
description = "Rust kernels for the Python-under-load measurement study (RQ1 compute, RQ2 branchy)"

[lib]
name = "mpkernels"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
